name: WorkFlow de despligue en rama main
on:
  push:
    branches:
      - main

jobs: 
  SAST:
    runs-on: ubuntu-latest
    steps:

      - name: Set up JDK 17
        uses: action/setup-java@v4.5.0
        with:
          java-version: '17'
          distribution: 'adopt'

      - name: Checkout code
        uses: actions/checkout@v4.2.2

      - name: Build
        run: |
          chmod 777 firebase


  BUILD:
    runs-on: ubuntu-latest
    steps:

      - name: Ejecutando WorkFlow
        run: | 
          echo 'Compilando...'

    TESTING:
    runs-on: ubuntu-latest
    steps:

      - name: TESTING
        run: | 
          echo 'TESTING'

    DEPLOY:
    runs-on: ubuntu-latest
    steps:

      - name: DEPLOY DSR
        run: | 
          echo 'DEPLOY DSR'
