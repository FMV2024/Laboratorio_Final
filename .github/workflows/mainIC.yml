name: WorkFlow de despligue en rama main
on:
  push:
    branches:
      - main

jobs: 
  BUILD:
    runs-on: ubuntu-latest
    steps:

      - name: Ejecutando WorkFlow
        run: | 
          echo 'Compilando...'

    TESTING:
    runs-on: ubuntu-latest
    steps:

      - name: TESTING
        run: | 
          echo 'TESTING'

    DEPLOY:
    runs-on: ubuntu-latest
    steps:

      - name: DEPLOY DSR
        run: | 
          echo 'DEPLOY DSR'
