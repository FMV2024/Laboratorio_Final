name: WorkFlow de despligue en rama main
on:
  push:
    branches:
      - main

jobs: 
  SAST:
    runs-on: ubuntu-24.04
    steps:

       - name: SonarCloud Implementación de Proyecto
        run: |
          curl -f -X POST -u '${{ secrets.TOKEN_SONAR}}:' 'https://sonarcloud.io/api/projects/create' -d 'name=${{github.event.repository.name }}' -d 'organization=fmv2024' -d 'visibility=public'


  BUILD:
    runs-on: ubuntu-24.04
    steps:

      - name: Ejecutando WorkFlow
        run: | 
          echo 'Compilando...'

    TESTING:
    runs-on: ubuntu-24.04
    steps:

      - name: TESTING
        run: | 
          echo 'TESTING'

    DEPLOY:
    runs-on: ubuntu-24.04
    steps:

      - name: DEPLOY DSR
        run: | 
          echo 'DEPLOY DSR'
